# Preserve a legacy reference baseline separately from current Installed Data

Migration will record digests and available version or provenance metadata for every legacy public reference resource so historical report claims can be reproduced and accounted for. Health Engine will separately acquire current Installed Data through its explicit update workflow and produce the current Analysis from that data; migration will explain claim differences caused by code corrections versus reference-data changes, while reference datasets remain outside the private Workspace database.
